# Cluster Autoscaler discovers node groups using tags.
# For EKS managed node groups, these tags are applied to the node group.
# If your organization requires explicit ASG tags, extend this file after reading ASG names from the node group resources.
